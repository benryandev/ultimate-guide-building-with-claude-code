# Git Conventions

- Write descriptive commit messages: `feat:`, `fix:`, `refactor:`, `docs:`, `chore:`, `style:`, `test:`
- One logical change per commit
- Never modify lockfiles manually (pnpm-lock.yaml, package-lock.json)
- Never commit .env files -- use .env.example for documentation
